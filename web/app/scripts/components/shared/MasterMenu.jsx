var React = require('react/addons');

var MasterMenu = React.createClass({
  render: function() {
    return (
      <div className="MasterMenu">
        <i className="fa fa-bars"></i>
      </div>
    );
  }
});

module.exports = MasterMenu;
