<%= form_for(@visit_record) do |f| %>
  <% if @visit_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit_record.errors.count, "error") %> prohibited this visit_record from being saved:</h2>

      <ul>
      <% @visit_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :visit_id %><br>
    <%= f.text_field :visit_id %>
  </div>
  <div class="field">
    <%= f.label :patient_id %><br>
    <%= f.text_field :patient_id %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_area :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
